<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_main_event_banner"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_above="@+id/tab_bottom_skin_lay"
    android:layout_alignParentRight="true"
    android:layout_marginBottom="20dp"
    android:layout_marginRight="20dp">

    <ImageView
        android:id="@+id/iv_main_event_close"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:src="@mipmap/ic_launcher_round" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_toLeftOf="@id/iv_main_event_close"
        android:visibility="visible">

        <RelativeLayout
            android:id="@+id/rl_main_event"
            android:layout_width="200dp"
            android:layout_height="match_parent"
            android:layout_marginRight="10dp"
            android:visibility="invisible">

            <TextView
                android:id="@+id/tv_main_event_text"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="#0f0"
                android:ellipsize="end"
                android:gravity="center"
                android:maxEms="6"
                android:paddingLeft="13dp"
                android:paddingRight="20dp"
                android:singleLine="true"
                android:text="获取更多信息"
                android:textColor="#ff0000"
                android:textSize="13dp" />
        </RelativeLayout>
    </RelativeLayout>


</RelativeLayout>